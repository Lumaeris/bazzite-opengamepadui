modules:
  - type: script
    scripts:
      - opengamepadui-session.sh
      - install-packages.sh

  - type: files
    files:
      - source: system
        destination: /

  - type: systemd
    system:
      enabled:
        - powerstation
        - inputplumber
        - inputplumber-suspend

  - type: signing
